---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: {{ service }}
spec:
  host: {{ service }}
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: {{ max_conn }}
      http:
        maxRequestsPerConnection: {{ requests }}
